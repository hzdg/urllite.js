urllite.js
==========

urllite is a URL parser for JavaScript. Its goal is to allow you to parse URLs
the same way in node and the browser, but be light enough to be bundled with
browser JS libraries.
